apiVersion: v1
kind: Service
metadata:
  name: apache_static_srv
  namespace: apache
spec:
  selector:
    app: apache_deployment
  type: ClusterIP
  sessionAffinity: None
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800
  ports:
  - name: apache_static_srv
    protocol: TCP
    port: 80
    targetPort: 80